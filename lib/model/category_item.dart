import 'package:flutter/material.dart';

class CategoryItem {
  final String name;
  final IconData icon;
  final Color color;
  final Color accentColor;

  CategoryItem(this.name, this.icon, this.color, this.accentColor);
}
